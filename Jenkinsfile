pipeline {
    agent any

    stages {
        stage('SCM Pull') {
            steps {
                                git branch: 'master', url: 'https://github.com/Kevinjosetom/Multibranch.git'

            }
        }
    }
}
