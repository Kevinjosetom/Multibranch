pipeline {
    agent any

    stages {
        stage('SCM Pull') {
            steps {
                                git branch: 'Dev', url: 'https://github.com/Kevinjosetom/Multibranch.git'

            }
        }
    }
}
