pipeline {
    agent any

    stages {
        stage('SCM Pull') {
            steps {

                                git branch: 'main', url: 'https://github.com/Kevinjosetom/Multibranch.git'

                                


            }
        }
    }
}
